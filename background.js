// HDB Status Checker
var hdbpages = [];
console.log("HDBScraper background.js init");

/*
chrome.extension.onMessage.addListener(
	function (request, sender, sendResponse) {
	chrome.pageAction.show(sender.tab.id);
	sendResponse();
});
*/